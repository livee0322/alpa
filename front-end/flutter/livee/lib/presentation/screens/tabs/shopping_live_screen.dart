import 'package:flutter/material.dart';

class ShoppingLiveScreen extends StatelessWidget {
  const ShoppingLiveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '쇼핑라이브 화면 (구현 예정)',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
